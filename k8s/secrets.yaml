apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  # This is a template. In a real CI/CD pipeline, these would be injected
  # or created manually in the cluster using:
  # kubectl create secret opaque app-secrets --from-literal=USER_DB_CONNECTION="..." ...
  USER_DB_CONNECTION: "placeholder"
  PET_DB_CONNECTION: "placeholder"
  JWT_KEY: "placeholder"
  COSMOS_URI: "placeholder"
  COSMOS_KEY: "placeholder"
